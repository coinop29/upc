[build]
  publish = "public"
  functions = "netlify/functions"

# Optional redirect rule so you can call /api/accountability directly
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200